from .app import app

# This file is required for Vercel deployment
# It imports the Flask app from app.py in the same directory
# and makes it available to the Vercel platform

# Vercel will automatically detect this file and use it as the entry point